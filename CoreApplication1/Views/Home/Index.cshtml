
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
</div>


<div class="container mt-3">
    <h2>Employee Table</h2>
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Department</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var emp in Model)
            {
                <tr>
                    <td>@emp.Id</td>
                    <td>@emp.Name
                        <img class="card-img-top" src="~/images/games.jpg" style="width:100px; height:50px; object-fit:cover;" />
                    </td>
                    <td>@emp.Department</td>
                </tr>
            
            }
          
            
        </tbody>
    </table>
    <div class="card-deck">
        @foreach(var employee in Model)
        {
            <div class="card m-3">
                <div class="card header">
                    <h3>@employee.Name</h3>
                </div>
                <img class="card-img-top" src="~/images/games.jpg" style="width:300px; height:100px; object-fit:cover;" />
                <div class="card-footer text-center">
                    <a asp-controller="Home" asp-action="Details" asp-route-id="@employee.Id"
                    class="btn btn-primary">
                    View</a>
                    <a asp-controller="Home"
                      class="btn btn-primary">
                     Edit</a>
                    <a asp-controller="Home" 
                     class="btn btn-danger">
                     Delete</a>
                  
                </div>

            </div>
        }

    </div>
  
</div>






















